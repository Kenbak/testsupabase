import { Component, createMemo, Show, Switch, Match, onMount } from "solid-js";
import { Atom, atom } from "solid-use";

const Footer: Component = () => {


  return (
    <nav  class="flex max-w-1256px my-24px px-4rem mx-auto bg-transparent backdrop-blur-md z-30 align-items-center justify-between">
      <h1>TESTING</h1>
    </nav>
  );
};

export default Footer;
